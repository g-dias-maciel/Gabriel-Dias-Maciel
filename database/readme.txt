This folder is used to keep the local changes made on the db even after running\
"docker-compose down"
This way we have a persistent db
The *.sql files are used to start the first db volume with the books/users mockup data and build the db schema